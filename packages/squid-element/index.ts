import { version as version_ } from "./version"
import SCIcon from "@squid-element/icon"

const version = version_

const components = [
  SCIcon,
]

export {
  SCIcon,
}

export default {
  components,
  version,
}
